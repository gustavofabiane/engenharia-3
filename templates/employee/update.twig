{% extends "base/admin.twig" %}

{% block content %}
    
    <h2 class="page-header">Novo Funcionário</h2>
    
    <form name="newEmployee" method="POST" action="{{ path_for('update-employee', {id: employee.id}) }}">
        <input type="hidden" readonly name="_METHOD" value="PUT">
        <input type="hidden" readonly name="validacao" value="updateUser">
        {% include 'employee/employee-form.twig' with {'update': 'true'} %}
        <div class="text-right">
            <a class="btn btn-default" href="{{ path_for('list-employees') }}">Cancelar</a>
            <button class="btn btn-primary">Salvar</button>
        </div>
    </form>

{% endblock %}